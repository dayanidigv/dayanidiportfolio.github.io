# OS cruft
.DS_Store
**/.DS_Store


# Heavy source PNGs for the infrastructure asset set — the optimized .webp
# versions (in assets/images/assets/*.webp) are what the site actually loads.
# Keep the PNGs locally as source, but don't bloat the repo with ~26MB.
# assets/images/assets/*.png

# wrangler local cache/state (account id, etc.)
.wrangler/
worker/.wrangler/
